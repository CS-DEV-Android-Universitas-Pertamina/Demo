<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.csdev.week5">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Week5">
        <activity android:name=".List.DetailActivity"></activity>
        <activity android:name=".Fragment.FragmentActivity" />
        <activity android:name=".List.ActivityRecyclerView" />
        <activity android:name=".List.ActivityListView" />
        <activity android:name=".LFO.Lfo3" />
        <activity android:name=".LFO.Lfo2" />
        <activity android:name=".LFO.Lfo1" />
        <activity android:name=".BarVolume.BarVolume" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>